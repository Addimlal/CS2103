DecList(
  ProcDec(
    main,
    DecList(),
    DecList(),
    StmList(
      CallStm(
        prozess,
        ExpList(
          OpExp(
            LST,
            OpExp(
              ADD,
              IntExp(34),
              IntExp(34)),
            IntExp(3434)),
          OpExp(
            LST,
            IntExp(34),
            IntExp(3434)),
          VarExp(
            ArrayVar(
              SimpleVar(kk),
              OpExp(
                MUL,
                IntExp(45),
                OpExp(
                  SUB,
                  IntExp(0),
                  IntExp(34))))))))))
